# ONLY PORT IN VUE JS

### Install the server environment
```sh
npm install 
```
### Install the client environment
```sh
cd src && npm install
```
### in src folder
```sh
gulp
```
using `gulp` to run 

